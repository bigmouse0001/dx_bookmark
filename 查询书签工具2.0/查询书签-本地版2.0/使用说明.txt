1. 安装python3

推荐Python 3.11.4

2. 安装必要依赖

pip install os
pip install sys
pip install json
pip install pyperclip
pip install sqlite3
pip install configparser
pip install re
pip install PySide6
pip install psutil
pip install subprocess

自己运行看看，提示缺什么module就pip install 什么

3. 按本机配置修改config文件

4. 脚本后缀改为.pyw，即可隐藏命令行窗口